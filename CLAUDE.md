# CLAUDE.md

이 저장소에서 Claude 가 지켜야 하는 규칙입니다.

## 배포는 따로, 지시가 있을 때만

**코드를 고쳤으면 검증하고 커밋까지만 하고 멈춥니다.** 배포하지 마세요.

"커밋까지" 에는 **push 도 포함되지 않습니다.** 이 앱은 사람들 PC 에 깔려 있고,
`main` 에 push 하는 순간 그 사람들 화면 위에 "새 버전이 있습니다" 띠가 뜹니다.
즉 push 자체가 이미 배포의 절반입니다. 반쯤 만든 묶음을 남에게 보이지 않으려면
push 도 배포할 때 같이 합니다.

그래서 한 번의 작업은 여기서 끝납니다:

1. 고친다
2. 검증한다 (화면이 걸린 변경이면 실제로 띄워서 본다)
3. 커밋한다
4. **멈추고, 무엇이 커밋됐는지 보고한다**

## 버전 (`package.json` 의 `version`)

화면 왼쪽 아래에 이 값이 뜹니다. 사용자가 "지금 몇 버전이냐" 를 볼 수 있는 **유일한**
자리라, 배포한 것과 적힌 것이 다르면 그 숫자는 없느니만 못합니다.

**배포할 때마다 올립니다.** `MAJOR.MINOR.PATCH`:

| 자리 | 언제 올리나 | 예 |
| --- | --- | --- |
| **MAJOR** | 기존 것이 그대로는 안 돌아갈 때. 데이터·설정 형식이 바뀌어 손봐야 하거나, 쓰던 것이 없어지거나 뜻이 달라질 때 | `~/.claude-crew` 구조 변경, 유형 파일 형식 변경, 쓰던 기능 제거 |
| **MINOR** | 없던 것이 생겼을 때. 있던 것은 그대로 | 중단 버튼 추가, 토끼 알림 추가 |
| **PATCH** | 있던 것이 제대로 안 되던 걸 고쳤을 때. 문구·스타일 손질도 여기 | 로그인 창이 안 뜨던 것, 받기가 안 먹던 것 |

애매하면 **낮은 쪽**으로 갑니다. 기능인지 고침인지 헷갈리는 것은 대체로 고침이고,
MAJOR 는 "쓰던 사람이 뭔가 해야 한다" 일 때만입니다.

한 번에 여러 커밋을 내보낼 때는 **그중 가장 높은 자리 하나만** 올립니다
(기능 2개 + 고침 3개 = MINOR 하나). 버전 올리는 커밋은 배포 묶음의 **마지막**에 둡니다 —
그래야 그 커밋이 곧 "여기까지가 이 버전" 이라는 표시가 됩니다.

## 배포하라는 지시가 오면

마지막 배포 이후 쌓인 커밋을 **한꺼번에** 내보냅니다. 지금 것만 따로 내보내지
않습니다.

**무엇이 밀려 있는지 아는 법** — 구동본의 HEAD 가 곧 "마지막으로 배포된 지점"
입니다. 저장소 어딘가의 기록이 아니라 그 폴더가 정답입니다:

```bash
cd "$LOCALAPPDATA/claude-crew" && git log --oneline -1     # 지금 돌고 있는 것
cd <개발본> && git log --oneline <그 커밋>..main            # 밀려 있는 것들
```

**내보내는 순서**:

0. **버전을 올린다** (위 표대로). 이 커밋이 묶음의 마지막이다 — 화면에 뜨는
   숫자와 실제로 나간 것이 여기서 맞춰진다.
1. `git push origin main`
2. 구동본을 최신으로: `curl -s -X POST http://127.0.0.1:4320/api/update`
3. **앱 재시작** — `src/*.js` 는 메모리에 올라가 있어서 받기만 해서는 안 바뀐다.
   포트 4320 을 잡고 있는 node 를 죽이고 `%LOCALAPPDATA%\claude-crew\src\index.js`
   를 다시 띄운다 (`scripts\launch.ps1 -Restart` 도 같은 일을 한다).
4. 뜬 화면에서 이번에 넣은 것이 실제로 보이는지 확인한다.
5. **묶음 전체를 보고한다** — 이번 배포에 무엇 무엇이 들어갔는지. 사용자는
   여러 번의 작업을 한 번에 내보내는 것이므로, 마지막 하나만 말하면 나머지가
   조용히 나간 것이 된다.

## 사본이 둘이라는 것

| | 어디 | 무엇 |
| --- | --- | --- |
| 개발본 | `Documents\claude-crew` | 여기서만 고친다 |
| 구동본 | `%LOCALAPPDATA%\claude-crew` | 바탕화면 아이콘·자동 업데이트가 보는 곳 |

데이터(`~/.claude-crew`)는 둘과 별개라 배포해도 업무 이력·유형 설정이 지워지지
않습니다.
